<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patient Health Summary - Hanna</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            font-size: 11pt;
            line-height: 1.5;
            color: #1E293B;
            background: #fff;
        }

        .page {
            width: 210mm;
            min-height: 297mm;
            padding: 15mm;
            margin: 0 auto;
            background: #fff;
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            border-bottom: 2px solid #3B82F6;
            padding-bottom: 12px;
            margin-bottom: 16px;
        }

        .header-title {
            font-size: 18pt;
            font-weight: 700;
            color: #0F172A;
        }

        .header-subtitle {
            font-size: 10pt;
            color: #64748B;
            margin-top: 4px;
        }

        .header-meta {
            text-align: right;
            font-size: 9pt;
            color: #64748B;
        }

        /* Section */
        .section {
            margin-bottom: 14px;
        }

        .section-title {
            font-size: 11pt;
            font-weight: 600;
            color: #1E40AF;
            margin-bottom: 8px;
            padding-bottom: 4px;
            border-bottom: 1px solid #E2E8F0;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .section-content {
            padding: 10px 12px;
            background: #F8FAFC;
            border-radius: 6px;
            border: 1px solid #E2E8F0;
        }

        /* Patient Snapshot Box */
        .snapshot-grid {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 12px;
        }

        .snapshot-item {
            display: flex;
            flex-direction: column;
        }

        .snapshot-label {
            font-size: 9pt;
            color: #64748B;
            font-weight: 500;
        }

        .snapshot-value {
            font-size: 11pt;
            font-weight: 600;
            color: #0F172A;
        }

        /* Metric Highlight */
        .metric-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 6px;
        }

        .metric-label {
            color: #475569;
        }

        .metric-value {
            font-weight: 600;
        }

        .metric-good {
            color: #16A34A;
        }

        .metric-warning {
            color: #D97706;
        }

        .metric-critical {
            color: #DC2626;
        }

        .metric-neutral {
            color: #0F172A;
        }

        /* Table */
        .data-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 10pt;
        }

        .data-table th {
            background: #E2E8F0;
            padding: 6px 10px;
            text-align: left;
            font-weight: 600;
            color: #475569;
        }

        .data-table td {
            padding: 6px 10px;
            border-bottom: 1px solid #E2E8F0;
        }

        /* Chart Container */
        .chart-container {
            margin: 10px 0;
            text-align: center;
        }

        .chart-container img {
            max-width: 100%;
            height: auto;
        }

        .chart-label {
            font-size: 9pt;
            color: #64748B;
            margin-top: 4px;
        }

        /* AI Summary Box */
        .ai-summary {
            background: #EFF6FF;
            border: 1px solid #BFDBFE;
            border-left: 4px solid #3B82F6;
            border-radius: 6px;
            padding: 12px;
        }

        .ai-summary-header {
            font-size: 9pt;
            font-weight: 600;
            color: #1E40AF;
            margin-bottom: 6px;
        }

        .ai-summary-content {
            font-size: 10pt;
            color: #1E293B;
            font-style: italic;
        }

        /* Trend Badge */
        .trend-badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 9pt;
            font-weight: 600;
        }

        .trend-improving {
            background: #DCFCE7;
            color: #166534;
        }

        .trend-stable {
            background: #E0E7FF;
            color: #3730A3;
        }

        .trend-worsening {
            background: #FEE2E2;
            color: #991B1B;
        }

        /* Footer */
        .footer {
            margin-top: 20px;
            padding-top: 12px;
            border-top: 1px solid #E2E8F0;
            font-size: 8pt;
            color: #64748B;
        }

        .footer-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 4px;
        }

        .disclaimer {
            margin-top: 10px;
            padding: 8px;
            background: #FEF3C7;
            border: 1px solid #FCD34D;
            border-radius: 4px;
            font-size: 8pt;
            color: #92400E;
            text-align: center;
        }

        /* Two Column */
        .two-col {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
        }

        /* Tag List */
        .tag-list {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
        }

        .tag {
            display: inline-block;
            padding: 2px 8px;
            background: #E2E8F0;
            border-radius: 4px;
            font-size: 9pt;
            color: #475569;
        }
    </style>
</head>

<body>
    <div class="page">
        <!-- Header -->
        <div class="header">
            <div>
                <div class="header-title">📋 Patient Health Summary</div>
                <div class="header-subtitle">Hanna AI Nurse Infrastructure</div>
            </div>
            <div class="header-meta">
                <div>Monitoring Period: <strong>Last <%= timeRange.days %> Days</strong></div>
                <div>
                    <%= new Date(timeRange.startDate).toLocaleDateString('en-US') %> - <%= new
                            Date(timeRange.endDate).toLocaleDateString('en-US') %>
                </div>
            </div>
        </div>

        <!-- 1. Patient Snapshot -->
        <div class="section">
            <div class="section-title">👤 1. Patient Snapshot</div>
            <div class="section-content">
                <div class="snapshot-grid">
                    <div class="snapshot-item">
                        <span class="snapshot-label">Patient ID</span>
                        <span class="snapshot-value">HN-<%= String(patient.id).padStart(8, '0' ) %></span>
                    </div>
                    <div class="snapshot-item">
                        <span class="snapshot-label">Age</span>
                        <span class="snapshot-value">
                            <%= patient.age || 'N/A' %> years
                        </span>
                    </div>
                    <div class="snapshot-item">
                        <span class="snapshot-label">Condition(s)</span>
                        <span class="snapshot-value">
                            <%= patient.condition || 'N/A' %>
                        </span>
                    </div>
                    <div class="snapshot-item">
                        <span class="snapshot-label">Name</span>
                        <span class="snapshot-value">
                            <%= patient.name || 'N/A' %>
                        </span>
                    </div>
                    <div class="snapshot-item">
                        <span class="snapshot-label">Status</span>
                        <span class="snapshot-value">
                            <%= patient.status || 'Active' %>
                        </span>
                    </div>
                    <div class="snapshot-item">
                        <span class="snapshot-label">Enrolled Since</span>
                        <span class="snapshot-value">
                            <%= new Date(patient.enrolledSince).toLocaleDateString('en-US') %>
                        </span>
                    </div>
                </div>
            </div>
        </div>

        <div class="two-col">
            <!-- 2. Engagement Overview -->
            <div class="section">
                <div class="section-title">💬 2. Engagement Overview</div>
                <div class="section-content">
                    <div class="metric-row">
                        <span class="metric-label">Days Responded</span>
                        <span
                            class="metric-value <%= engagement.percentage >= 70 ? 'metric-good' : engagement.percentage >= 50 ? 'metric-warning' : 'metric-critical' %>">
                            <%= engagement.daysResponded %> / <%= engagement.totalDays %> days (<%=
                                        engagement.percentage %>%)
                        </span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Missed Check-ins</span>
                        <span class="metric-value metric-neutral">
                            <%= engagement.missedDays %> days
                        </span>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">LINE Engagement</span>
                        <span class="metric-value metric-good">Confirmed ✓</span>
                    </div>
                </div>
            </div>

            <!-- 3. Medication Adherence -->
            <div class="section">
                <div class="section-title">💊 3. Medication Adherence</div>
                <div class="section-content">
                    <div class="metric-row">
                        <span class="metric-label">Adherence Rate</span>
                        <% if (adherence.percentage !==null) { %>
                            <span
                                class="metric-value <%= adherence.percentage >= 80 ? 'metric-good' : adherence.percentage >= 60 ? 'metric-warning' : 'metric-critical' %>">
                                <%= adherence.percentage %>%
                            </span>
                            <% } else { %>
                                <span class="metric-value metric-neutral">No data</span>
                                <% } %>
                    </div>
                    <div class="metric-row">
                        <span class="metric-label">Doses Taken</span>
                        <span class="metric-value metric-neutral">
                            <%= adherence.totalTaken %> / <%= adherence.totalExpected %>
                        </span>
                    </div>
                    <% if (adherence.pattern) { %>
                        <div style="margin-top: 8px; font-size: 9pt; color: #D97706;">
                            ⚠️ <%= adherence.pattern %>
                        </div>
                        <% } %>
                </div>
            </div>
        </div>

        <!-- 4. Symptom & Risk Signals -->
        <div class="section">
            <div class="section-title">⚠️ 4. Symptom & Risk Signals</div>
            <div class="section-content">
                <div class="two-col">
                    <div>
                        <div class="metric-row">
                            <span class="metric-label">Reported Symptoms</span>
                            <span class="metric-value metric-neutral">
                                <%= symptoms.total %> occurrences
                            </span>
                        </div>
                        <% if (symptoms.list.length> 0) { %>
                            <div class="tag-list" style="margin-top: 8px;">
                                <% symptoms.list.slice(0, 5).forEach(s=> { %>
                                    <span class="tag">
                                        <%= s.name %> (<%= s.count %>)
                                    </span>
                                    <% }); %>
                            </div>
                            <% } else { %>
                                <div style="font-size: 9pt; color: #64748B; margin-top: 4px;">No symptoms reported</div>
                                <% } %>
                    </div>
                    <div>
                        <div class="metric-row">
                            <span class="metric-label">Trend</span>
                            <span class="trend-badge trend-<%= vitals.trend %>">
                                <%= vitals.trend==='improving' ? '↗ Improving' : vitals.trend==='worsening'
                                    ? '↘ Worsening' : '→ Stable' %>
                            </span>
                        </div>
                        <div class="metric-row" style="margin-top: 8px;">
                            <span class="metric-label">Nurse Escalations</span>
                            <span
                                class="metric-value <%= escalations.critical > 0 ? 'metric-critical' : 'metric-neutral' %>">
                                <%= escalations.total %>
                                    <% if (escalations.critical> 0) { %>(Critical: <%= escalations.critical %>)<% } %>
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- 5. Trend Visualization -->
        <div class="section">
            <div class="section-title">📊 5. Trend Visualization</div>
            <div class="section-content">
                <div class="two-col">
                    <div class="chart-container">
                        <% if (charts.adherenceChart) { %>
                            <img src="<%= charts.adherenceChart %>" alt="Adherence Trend">
                            <div class="chart-label">Medication Adherence (%)</div>
                            <% } else { %>
                                <div style="padding: 20px; color: #64748B;">No chart data available</div>
                                <% } %>
                    </div>
                    <div class="chart-container">
                        <% if (charts.glucoseChart) { %>
                            <img src="<%= charts.glucoseChart %>" alt="Glucose Trend">
                            <div class="chart-label">Blood Glucose (mg/dL)</div>
                            <% } else { %>
                                <div style="padding: 20px; color: #64748B;">No glucose data available</div>
                                <% } %>
                    </div>
                </div>
            </div>
        </div>

        <!-- 6. Nurse Notes -->
        <div class="section">
            <div class="section-title">👩‍⚕️ 6. Nurse Notes & Actions</div>
            <div class="section-content">
                <% if (nurseActions.recentTasks.length> 0) { %>
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Type</th>
                                <th>Details</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% nurseActions.recentTasks.forEach(task=> { %>
                                <tr>
                                    <td>
                                        <%= new Date(task.created_at).toLocaleDateString('en-US') %>
                                    </td>
                                    <td>
                                        <%= task.task_type || 'General' %>
                                    </td>
                                    <td>
                                        <%= task.reason || task.title || '-' %>
                                    </td>
                                    <td>
                                        <%= task.resolved_at ? '✓ Resolved' : '⏳ Pending' %>
                                    </td>
                                </tr>
                                <% }); %>
                        </tbody>
                    </table>
                    <% } else { %>
                        <div style="text-align: center; padding: 12px; color: #64748B;">
                            No nurse actions recorded during this period
                        </div>
                        <% } %>
            </div>
        </div>

        <!-- 7. AI Summary -->
        <div class="section">
            <div class="section-title">🤖 7. AI Summary</div>
            <div class="ai-summary">
                <div class="ai-summary-header">AI-Generated Summary (Reviewed by Clinical Team)</div>
                <div class="ai-summary-content">
                    "<%= aiSummary %>"
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div class="footer-row">
                <span>Generated: <%= new Date(generatedAt).toLocaleString('en-US') %></span>
                <span>Audit ID: <%= auditId %></span>
            </div>
            <div class="footer-row">
                <span>Document ID: PDF-<%= new Date().getFullYear() %>-<%= String(Math.random()).slice(2, 8) %></span>
                <span>Hanna AI Nurse Infrastructure</span>
            </div>
            <div class="disclaimer">
                ⚠️ This summary supports clinical decision-making but does not replace clinician judgment.
            </div>
        </div>
    </div>
</body>

</html>